配置采用了optix 5.1      cuda 9.1 版本， 其中部分dll需要有visual 2017 distribution的环境（一般大家的电脑都是有的，出现dll问题后可以考虑这方面的问题）。
如果版本不一样的话应该是只要修改项目属性里面的配置即可，把相应的配置全都改到你电脑的对应版本。
另外Optix和cuda需要安装在默认目录下，如有改动，依然修改项目属性的配置就可解决。（应该是的）

注意： shader支持的最多纹理数量仅为32，其他多绑的都是无效的。

运行时偶尔会突然中断，大部分都是由于optix引起的，原因未知，目前只能略过不管。